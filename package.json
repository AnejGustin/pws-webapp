{
  "name": "pws-website",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build -w packages/shared && npm run build -w packages/db && concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/db && npm run build -w apps/api && npm run build -w apps/web"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AnejGustin/pws-website.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/AnejGustin/pws-website/issues"
  },
  "homepage": "https://github.com/AnejGustin/pws-website#readme",
  "description": "",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}